package com.example.cryptomarket.ui.login

import android.os.Bundle
import androidx.appcompat.app.AppCompatActivity
import androidx.navigation.fragment.NavHostFragment
import com.example.cryptomarket.R
import com.example.cryptomarket.databinding.ActivityLoginBinding
import com.example.cryptomarket.databinding.ActivityMainMenuBinding

class LoginActivity: AppCompatActivity() {
//    private lateinit var binding: ActivityLoginBinding
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_login)
//        binding = ActivityLoginBinding.inflate(layoutInflater)
//        setContentView(binding.root)

//        val navHost = supportFragmentManager.findFragmentById(R.id.fragmentLoginContainer) as NavHostFragment
//        val navController = navHost.navController
    }
}